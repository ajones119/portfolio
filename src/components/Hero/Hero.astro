---
import Avatar1 from "../../assets/images/aramis/LinkdInAvatar.jpg"
import Avatar2 from "../../assets/images/aramis/aramisnarwen.png"
---

<section class="h-screen ">
    <div class="h-6/12 flex items-center justify-center">
        <div class="max-w-4xl mx-auto">
            <h1 class="text-4xl font-bold font-abril-fatface text-center">Hello World.</h1>
            <p class="text-lg text-center font-playfair-display">I'm Aramis, this is my technical playground.</p>
        </div>
    </div>
    <div class="h-4/12 flex items-center justify-center">
        <div class="max-w-4xl mx-auto">
            <div id="next-section-button-container" class="absolute bottom-0 left-1/2 -translate-x-1/2 size-40">
                <div class="absolute top-0 left-0 w-full h-full">
                    <svg id="hero-svg" class="" width="100%" height="100%" viewBox="0 0 48 48" fill="none" stroke="currentColor">
                        {/* dots around button, these will explode on click */}
                        <circle class="flash" cx="50%" cy="50%" r="0" fill="currentColor" stroke="currentColor"/>
                        <circle class="dot" cx="50%" cy="50%" r="0.25" fill="currentColor" stroke="currentColor"/>
                        <circle class="dot" cx="50%" cy="50%" r="0.25" fill="currentColor" stroke="currentColor"/>
                        <circle class="dot" cx="50%" cy="50%" r="0.25" fill="currentColor" stroke="currentColor"/>
                        <circle class="dot" cx="50%" cy="50%" r="0.25" fill="currentColor" stroke="currentColor"/>
                        <circle class="dot" cx="50%" cy="50%" r="0.25" fill="currentColor" stroke="currentColor"/>
                        <circle class="dot" cx="50%" cy="50%" r="0.25" fill="currentColor" stroke="currentColor"/>
                        <circle class="dot" cx="50%" cy="50%" r="0.25" fill="currentColor" stroke="currentColor"/>
                        <circle class="dot" cx="50%" cy="50%" r="0.25" fill="currentColor" stroke="currentColor"/>
                        <circle class="dot" cx="50%" cy="50%" r="0.25" fill="currentColor" stroke="currentColor"/>
                        <circle class="dot" cx="50%" cy="50%" r="0.25" fill="currentColor" stroke="currentColor"/>
                        <circle class="dot" cx="50%" cy="50%" r="0.25" fill="currentColor" stroke="currentColor"/>
                        <circle class="dot" cx="50%" cy="50%" r="0.25" fill="currentColor" stroke="currentColor"/>
                    </svg>
                </div>
                <a href="#about-me" class="absolute bottom-6/12 translate-y-6/12 left-1/2 -translate-x-1/2 cursor-pointer transition-colors duration-300 bg-background hover:bg-primary/80 hover:text-background border border-solid border-primary text-primary rounded-full size-8 justify-center flex items-center">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        {/* down arrow centered */}
                        <path d="M19 10L12 16 5 10"/>
                        
                    </svg>
                </a>
            </div>
        </div>
    </div>
</section>

<script>
    import { initNextSectionButton } from './Hero';
    
    initNextSectionButton();
    
</script>