---
import Layout from './Layout.astro';

interface Props {
	title: string;
	description?: string;
}

const { title, description } = Astro.props;
const pageTitle = `${title} • Aramis Jones`;
---

<Layout title={pageTitle} description={description || title} consoleMessage="wow so learn, much smart">
	<slot />
</Layout>

