# Teaching notes

- User asked why JS already has event emitters but the project uses a custom class — misconception corrected: DOM/Node emitters serve different layers than app-class pub/sub.
- User is actively building `containedProject/` with `Experience`, `Sizes`, and `EventEmitter`.
- Prefers explanations tied to their actual code, not abstract examples.
